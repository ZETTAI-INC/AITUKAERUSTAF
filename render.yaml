services:
  - type: web
    name: otasuke-ai
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NODE_ENV
        value: production
