services:
  - type: web
    name: otasuke-ai
    runtime: node
    buildCommand: npm install -g serve
    startCommand: serve -s . -l $PORT
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NODE_ENV
        value: production
